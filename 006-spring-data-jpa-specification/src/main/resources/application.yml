spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/state_division
    username: root
    password: admin
    
  jpa:
    hibernate:
      ddl-auto: drop-and-create
    properties:
      hibernate:
        '[show_sql]': true
        '[format_sql]': true